## 包

> 一个目录下的同级文件归属一个包，package的声明要一致
>
> package声明的包和对应的目录名可以不一致，但习惯上还是写成一致的
>
> 包可以嵌套
>
> 同包下的函数不需要导入包，可以直接使用
>
> main包，main()函数所在的包，其他的包不能使用
>
> 导入包的时候，路径要从src下开始写

定义结构体时，结构体名和字段名的首字母大写意味着可以从外部访问，小写则不能访问



init函数和main函数

1.这两个函数都是go语言中的保留函数，init()用于初始化信息，main()用于作为程序的入口

2.这两个函数在定义的时候，不能有参数、返回值，只能由go程序自动调用，不能被引用

3.init()函数可以定义在任意包中，可以用多个，main()函数只能在main包下，并且只能有一个

4.对于不同包下：

​			A：先执行init函数，后执行main函数

​			B：对于同一个go文件下。调用顺序是从上到下，也就是说，先写的先被执行，后写的后被执行

​			C：对于同一个包下，将文件名按照字符串进行排序，之后顺序调用各个文件中的init函数

​			D：对于不同包下：

​					如果不存在依赖，按照main包中import的顺序来调用对应包中的init函数

​					如果存在依赖，最后被依赖的最先被初始化

​							导入顺序：main--->A--->B--->C

​							执行顺序：C--->B--->A--->main

5.存在依赖的包之间，不能循环存入

6.一个包可以被其他多个包import，但是只能被初始化一次

7." _ " 操作，其实是引入该包，而不直接使用该包里面的函数，仅仅是调用了该包的init函数

